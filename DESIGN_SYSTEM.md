# MiCoding 视觉规范

## 字体

拉丁字符优先使用 Avenir Next，以接近 Options+ 的 Brown Pro 字面与比例；中文统一回退到 macOS 的 PingFang SC。常规界面信息不低于 13 pt，12 pt 仅用于遥控器硬件标注等高密度信息，11 pt 只允许出现在电量数字或原始 HID Usage 一类技术元数据中。

| 层级 | 字号 / 字重 | 使用场景 |
| --- | --- | --- |
| Display | 33 / Bold | 首页问候语 |
| Page Title | 27 / Bold | 设置页标题 |
| Content Title | 24 / Bold | 设备页、自动化页标题 |
| Section Title | 20 / Semibold | 面板标题、主要内容分区 |
| Title | 16 / Semibold | 设备名、列表主文字 |
| Body | 14 / Regular | 正文、输入框、操作文字 |
| Body Medium | 14 / Medium | 主操作和重要标签 |
| Label | 13 / Semibold | 导航、标签、状态 |
| Supporting | 13 / Regular | 辅助说明和次级信息 |
| Hardware Detail | 12 / Regular | 遥控器按键名、紧凑标注 |

## 图标

应用界面统一使用 Lucide 24 × 24 线性图标，不混用 SF Symbols、彩色 Emoji 或不同笔画体系。Lucide 源图标保持 2 px 圆角描边，进入界面后只按光学尺寸缩放，不单独修改线宽。

- 页面和特征图标：20 pt
- 工具栏与导航：18 pt
- 列表动作图标：16 pt
- 输入框和紧凑控件：15 pt
- 箭头、勾选等指示图标：11 pt
- 图标与同级文字的水平间距：8 pt；紧凑控件可收至 6 pt。
- 独立图标按钮的可见图形通常为 18 pt，点击目标不得小于 36 × 36 pt。
- 默认使用 `currentColor` 继承文字颜色；仅成功、警告、选中等明确语义状态使用语义色。
- 同一语义只保留一个固定图标，例如设置始终使用 `settings`、智能操作始终使用 `workflow`。
- 遥控器产品图中的实体按键符号属于硬件外观，不替换成 Lucide，也不作为应用导航图标复用。
- 若图标资源缺失，显示中性占位符并在测试中失败，不回退到 SF Symbols。

## 尺寸与间距

- 以 4 pt 为基础网格，常用间距为 8、12、16、24、32、40、48 pt。
- 普通控件高 40 pt，紧凑控件高 36 pt。
- 页内文字操作高 32 pt，状态标签高 30 pt。
- 列表行高 64 pt，较宽松的功能行高 76 pt。
- 设备和设置页头高 84 pt，首页/智能操作展示页头高 98 pt。
- 设备侧边导航项高 72 pt，设置侧边导航项高 56 pt。
- 所有自定义点击目标不小于 40 × 40 pt；极紧凑的关闭按钮可保持 32 × 32 pt，但必须有清晰的辅助功能标签。
- 窗口最小尺寸为 1080 × 700 pt，主验收尺寸为 1280 × 820 pt。

## 导航与操作

- 首页、智能操作与设置属于同一顶层导航；进入设置时记录来源，返回后回到原页面。
- 设备配置页使用独立返回按钮；按键与设备信息保持在同一左侧导航中切换。
- 一个内容区域只保留一个主按钮。主操作使用实心强调色，次操作使用中性描边，行内操作只使用强调色文字。
- 侧栏选中态统一使用浅层表面、3 pt 强调色边线和强调色文字，悬停只增加低对比度中性底色。
- 悬停状态不得改变控件或内容的位置、尺寸和旋转；主页设备产品图保持完全静止。

## 颜色

- 浅色外观使用紫色作为交互强调色；深色外观使用薄荷青，确保深色表面上的轮廓和状态仍然清晰。
- 绿色只表达成功或权限已允许，橙色只表达提醒；不把语义色用于普通装饰。
- 大面积背景保持低饱和中性灰，高饱和强调色只用于选中边线、主按钮和短文本操作。

## 表面与层级

- Canvas 作为冷灰底色，Surface 承载主要内容，Elevated Surface 仅用于选中、搜索和可交互控件。
- 边框只用于表达容器和交互边界，不使用装饰性分隔线。
- 同一容器不叠加多重阴影。普通卡片无阴影，仅产品图和浮层使用低对比度阴影。
- 选中态使用“中性表面 + 强调色”，不用大面积高饱和色块。
- 自动化页面采用独立工具栏、分类侧栏和三列中性动作卡片；卡片使用 8 pt 圆角和固定低对比度阴影，不使用多色底、渐变卡片、步骤胶囊或悬浮抬升。
- 自动化步骤优先显示真实应用图标，抽象系统操作才使用 Lucide；分类选中态使用中性表面和 3 pt 强调线，不使用大面积强调色。

## 硬件映射

- 可点击热区必须跟随实体控件的轮廓，不得用通用圆角矩形代替方向环、摇杆等特殊结构。
- 方向环拆分为四个无重叠的环形扇区，中心确认键保持独立圆形命中区。
- 组合控件共用同一物理轮廓。音量加减使用一个胶囊摇杆的上、下半区，不渲染为两个独立按键。
- 设备配置页只展示可交互控制区；完整产品外观只在设备首页出现。
- 同一个当前按键和已分配动作只显示一次，由右侧动作面板承担，不在主视图底部重复。
